Notes:
What to still do:
    models:

    services:

    controllers: 
        authController:
        reportController:

        messageController:
        
there's still a few things here and there, but it's not major tings. i havne't tested the apis yet.

how to get it to run:
    node server (in terminal) OR just run in the top left corner
install:
    postman (app)
    
    vsc dependancies and scripts:
        npm install bcrypt
        npm install crypto-js
        npm install dotenv
        npm install express
        npm install jsonwebtoken
        npm install sequelize
        npm install tedious
        npm install nodemailer

    microsoft sql server:
        follow email instructions
    
don't forget to undo seeders and migrations!!


